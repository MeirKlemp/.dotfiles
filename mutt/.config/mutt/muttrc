set imap_user=meirk251@outlook.com

set folder=imaps://imap.outlook.com/

set spoolfile=+INBOX

set imap_check_subscribed

set header_cache = ~/.cache/mutt

set postponed = +/Drafts

unset imap_passive

set imap_keepalive = 300

set mail_check = 120
